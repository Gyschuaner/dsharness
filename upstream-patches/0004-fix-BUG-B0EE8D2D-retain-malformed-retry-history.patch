From c85a137b0d434ff81551e059a65f438c6310dd30 Mon Sep 17 00:00:00 2001
From: guyisheng <guyisheng@senzadmindeMacBook-Air.local>
Date: Wed, 19 Aug 2026 14:20:37 +0800
Subject: [PATCH 04/14] fix(BUG-B0EE8D2D): retain malformed retry history

---
 .../client/ui-conversation/README.i18n.yaml   |   4 +-
 packages/client/ui-conversation/README.md     |   2 +-
 packages/client/ui-conversation/README.zh.md  |   2 +-
 .../client/conversation-nodes/assistant.ts    |   7 +-
 ...nversation-node-definitions.client.spec.ts |  72 +++++++++++++
 packages/core/agent-loop/README.i18n.yaml     |   4 +-
 packages/core/agent-loop/README.md            |   8 +-
 packages/core/agent-loop/README.zh.md         |   8 +-
 packages/core/agent-loop/src/agent.ts         | 101 ++++++++++++------
 packages/core/agent-loop/src/invariant.ts     |   4 +-
 .../agent-loop/src/malformed-tool-call.ts     |  43 +++-----
 .../core/agent-loop/tests/invariant.spec.ts   |  23 ++--
 packages/core/agent-loop/tests/loop.spec.ts   |  59 ++++++++--
 packages/llm/llm-pi-ai/tests/adapter.spec.ts  |  34 +++++-
 packages/llm/llm-retry/README.i18n.yaml       |   4 +-
 packages/llm/llm-retry/README.md              |   2 +-
 packages/llm/llm-retry/README.zh.md           |   2 +-
 packages/llm/llm-retry/tests/retry.spec.ts    |  14 ++-
 packages/llm/llm/README.i18n.yaml             |   4 +-
 packages/llm/llm/README.md                    |   2 +-
 packages/llm/llm/README.zh.md                 |   2 +-
 packages/llm/llm/src/error.ts                 |   8 +-
 22 files changed, 286 insertions(+), 123 deletions(-)

diff --git a/packages/client/ui-conversation/README.i18n.yaml b/packages/client/ui-conversation/README.i18n.yaml
index 2b24d725a5..4590a51421 100644
--- a/packages/client/ui-conversation/README.i18n.yaml
+++ b/packages/client/ui-conversation/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-conversation/README.md
-README.md: 96f67677f690f12929cf691ab5901ded399504d3
-README.zh.md: 05fdcd7a989fb74eb9f86f11cffb4eb77d38088a
+README.md: 04d8f8db8de1ee1540167a67ec414169519e95cd
+README.zh.md: 95fbb880f476eed3f5191cc68b5904a99b63d5a1
diff --git a/packages/client/ui-conversation/README.md b/packages/client/ui-conversation/README.md
index 96f67677f6..04d8f8db8d 100644
--- a/packages/client/ui-conversation/README.md
+++ b/packages/client/ui-conversation/README.md
@@ -24,7 +24,7 @@ A Think row stays collapsed by default and exposes live reasoning throughput wit
 
 The chat view keeps Tool placement but delegates Tool presentation. Each ordered `tool-call` Conversation Node dispatches through the matching key of `conversation.chat.node`, while the details shell passes the selected call through `conversation.details.tool`. The assembled Web bundle registers [`ui-tool`](../ui-tool/README.md) for that Chat Node key; it renders the Runtime-projected recursive root/child tree and owns per-name dispatch, generic rendering, and render-intent cards. The details seat alone retains a raw-result fallback when that renderer is absent. A path click through the injected `openFile` asks the Host to open that path (relative paths resolve against the session cwd). A Host or OS refusal opens an in-page dialog with the thrown reason and a Retry of the same path; Cancel, Escape, the close control, and a mask click dismiss it ([decision](../../../.agents/notes/implemented/bug-fix/2026-08-18-tool-row-file-open-failure.md)).
 
-The chat flow projects each producer-correlated retry chain into one stable, muted status row updated to the latest attempt; every retry event remains in the runtime snapshot and session log. Its frontend countdown anchors the scheduled delay to client receipt, avoiding host/browser clock skew, rounds remaining time up to seconds, and has a one-second floor. The latest unresolved retry uses a left-to-right text shimmer. Subsequent turn facts distinguish an attempt that started from one cancelled during backoff, while the Host running bit only controls the live animation; the row then shows a static completed or cancelled label. Normal policy rows show the finite retry maximum; always policy rows show `∞`. Activating the row reveals the latest exact retry delay and failure message. The client runtime removes each failed attempt's streaming tail before its retry node arrives, while the status remains visible after a later attempt succeeds. A terminal failure renders as a persistent inline status at its turn boundary — beside the settled retry row when retries exhausted — showing the display-safe durable message and optional error code without offering an action the Host cannot fulfill; AUTH copy never echoes provider-supplied credential fragments.
+The chat flow projects each producer-correlated retry chain across retry turns into one stable, muted status row updated to the latest attempt; every retry event remains in the runtime snapshot and session log. Its frontend countdown anchors the scheduled delay to client receipt, avoiding host/browser clock skew, rounds remaining time up to seconds, and has a one-second floor. The latest unresolved retry uses a left-to-right text shimmer. Subsequent turn facts distinguish an attempt that started from one cancelled during backoff, while the Host running bit only controls the live animation; the row then shows a static completed or cancelled label. Normal policy rows show the finite retry maximum; always policy rows show `∞`. Activating the row reveals the latest exact retry delay and failure message. The client runtime removes a failed transport attempt's uncommitted streaming tail before its retry node arrives. When a semantic retry follows a completed but invalid assistant message, that settled Think remains visible in its original step and the corrective output owns the next step. The status remains visible after a later attempt succeeds. A terminal failure renders as a persistent inline status at its turn boundary — beside the settled retry row when retries exhausted — showing the display-safe durable message and optional error code without offering an action the Host cannot fulfill; AUTH copy never echoes provider-supplied credential fragments.
 
 `TodoDock` takes the `'conversation.input.dock'` list slot at `order: 0` — before Goal and Queue — and is the plan strip: it reads the host-computed `todos` projection via `useProjection` (standing plan: latest `todo/write` with no later `turn/start`) and renders `TodoPanel`, which takes the plain list, hides itself while the list is empty, and starts collapsed as a header of title plus its own `·`-joined per-status counts (localized, `1 completed · 2 in progress · 1 pending`, zero-count segments omitted). The dock adapter owns selection so the panel stays a pure function of its props. Anything the input-zone composer chain hides (a `conversation.composer` takeover such as ui-user-questions's) hides the whole dock, this strip included. The `todo_write` Tool row belongs to [`ui-tool`](../ui-tool/README.md).
 
diff --git a/packages/client/ui-conversation/README.zh.md b/packages/client/ui-conversation/README.zh.md
index 05fdcd7a98..95fbb880f4 100644
--- a/packages/client/ui-conversation/README.zh.md
+++ b/packages/client/ui-conversation/README.zh.md
@@ -22,7 +22,7 @@ Think 行默认保持折叠，并在不展开思维链的情况下暴露实时
 
 聊天视图保留工具的消息流位置，但委托其展示。每个已排序的 `tool-call` Conversation Node 都通过 `conversation.chat.node` 的同名 key 分发；详情壳层则通过 `conversation.details.tool` 传递当前选中的调用。组装后的 Web bundle 为该 Chat Node key 注册 [`ui-tool`](../ui-tool/README.zh.md)，由后者渲染运行时已投影的递归 root/child 树，并负责按名称分发、通用展示和 render-intent 卡片；只有详情席位会在该 renderer 缺席时保留 raw-result fallback。经注入的 `openFile` 点击路径会请 Host 打开该路径（相对路径按会话 cwd 解析）。Host 或操作系统拒绝时，页面内对话框展示抛出的原因，并提供对同一路径的重试；取消、Escape、关闭控件和点击遮罩会关掉对话框（[决策](../../../.agents/notes/implemented/bug-fix/2026-08-18-tool-row-file-open-failure.zh.md)）。
 
-聊天流会把每条生产方关联的重试链投影为一个稳定的弱化状态行，并用最新一次尝试更新该行；每个重试事件仍保留在运行时快照与会话日志中。前端倒计时以客户端收到事件的时刻为计划延迟的起点，避免 Host 与浏览器的时钟偏差；剩余时间向上取整到秒，且下限为 1 秒。最近一次尚未完成的重试会显示从左到右的文字渐变动画。后续轮次事实用于区分已开始的尝试与在退避期间取消的尝试，Host 的 running 位只控制实时动画；随后该行会显示静态的已完成或已取消标签。normal 策略行显示有限重试上限；always 策略行显示 `∞`。激活该行会显示最近一次重试的精确延迟和失败消息。客户端运行时会在相应重试节点到达前移除每次失败尝试的流式输出尾部；后续某次尝试成功后，该状态仍保持可见。终态失败会在其轮次边界渲染为持久的内联状态——重试耗尽后与定格的重试行并列——展示适合显示的持久消息与可选错误码，但不会提供 Host 无法兑现的操作；AUTH 文案绝不会回显提供方给出的凭据片段。
+聊天流会把每条生产方关联、跨重试轮次延续的重试链投影为一个稳定的弱化状态行，并用最新一次尝试更新该行；每个重试事件仍保留在运行时快照与会话日志中。前端倒计时以客户端收到事件的时刻为计划延迟的起点，避免 Host 与浏览器的时钟偏差；剩余时间向上取整到秒，且下限为 1 秒。最近一次尚未完成的重试会显示从左到右的文字渐变动画。后续轮次事实用于区分已开始的尝试与在退避期间取消的尝试，Host 的 running 位只控制实时动画；随后该行会显示静态的已完成或已取消标签。normal 策略行显示有限重试上限；always 策略行显示 `∞`。激活该行会显示最近一次重试的精确延迟和失败消息。客户端运行时会在相应重试节点到达前移除失败传输尝试尚未提交的流式输出尾部；语义重试若发生在一条已完成但无效的 assistant 消息之后，该条已结算 Think 会继续显示在原步骤中，纠错输出则归属下一步骤。后续某次尝试成功后，重试状态仍保持可见。终态失败会在其轮次边界渲染为持久的内联状态——重试耗尽后与定格的重试行并列——展示适合显示的持久消息与可选错误码，但不会提供 Host 无法兑现的操作；AUTH 文案绝不会回显提供方给出的凭据片段。
 
 审批通过本包声明的链条接管编辑器：`ApprovalPanel` 注册为按选择器路由的 `'conversation.composer'` 配置项（ui-user-questions 模式），在审批等待未决期间取代 InputBar 占据编辑器（琥珀色条、理由标题、来自运行中调用参数的配对命令行、一次性的拒绝／允许）。`contract/slots.ts` 中的 `PendingApproval` 领域面在运行时 `PendingWait` 载体之上拥有 wire 编码——带审计关联的 `ApprovalResponsePayload` 值；广播的 `approval/resolved` 帧使等待落定并恢复编辑器。运行时 manager 会将所有审批或问题等待通过 `SessionSummary.pendingInteraction` 投影出来，未实例化的会话也不例外；`ui-workspace` 负责其侧边栏呈现。未决等待完全离开消息流：问题（ui-user-questions）与审批（ApprovalPanel）都经编辑器接管作答，不再保留只读占位卡。编辑器底行的 Access 席位挂载 `PermissionSelect`，由 host 计算的 `permissions` 投影经标准工具包 `useProjection` 供数（key 缺席即隐藏 chip）；chip 打开 Menu 原语下拉，其中 kebab-case 预设名渲染为 Title Case 标签；普通安全预设会立即经输入栏注入的 `command` 回调提交 `/permission <preset>`，而 `danger-full-access` 在界面中显示为 `Full access`，选择后先打开页面内的 Modal 风险确认。用户勾选确认项前启用按钮始终不可用；取消、Escape、关闭按钮与点击遮罩都不会提交命令。
 
diff --git a/packages/client/ui-conversation/src/client/conversation-nodes/assistant.ts b/packages/client/ui-conversation/src/client/conversation-nodes/assistant.ts
index df8f87ee53..5b3cfe3056 100644
--- a/packages/client/ui-conversation/src/client/conversation-nodes/assistant.ts
+++ b/packages/client/ui-conversation/src/client/conversation-nodes/assistant.ts
@@ -201,7 +201,7 @@ function fallbackState(context: ConversationNodeContext<AssistantState>): Assist
       continue
     }
     if (match.event.type === 'llm/retry' && state !== undefined) {
-      state = resetForRetry(state)
+      state = state.final === undefined ? resetForRetry(state) : state
     }
   }
   return state
@@ -272,7 +272,10 @@ export const assistantDefinition: ConversationNodeDefinition<AssistantState> = {
       }
     }
     if (match.event.type === 'llm/retry') {
-      return resetForRetry(context.state)
+      // Transport retries have no committed message and replace the partial
+      // stream in this step. A semantic retry after a completed-but-invalid
+      // assistant message starts a new step, so keep this settled Think visible.
+      return context.state.final === undefined ? resetForRetry(context.state) : context.state
     }
     return context.state
   },
diff --git a/packages/client/ui-conversation/tests/conversation-node-definitions.client.spec.ts b/packages/client/ui-conversation/tests/conversation-node-definitions.client.spec.ts
index 262d2e73e1..a2d64e6f50 100644
--- a/packages/client/ui-conversation/tests/conversation-node-definitions.client.spec.ts
+++ b/packages/client/ui-conversation/tests/conversation-node-definitions.client.spec.ts
@@ -192,6 +192,78 @@ describe('built-in conversation node Definitions', () => {
     ])
     expect(node(snapshot(hiddenValue), 'assistant-step')).toBeUndefined()
 
+    const semanticRetryValue = assembler([
+      at(30, 'turn/start', { turn: 4 }),
+      at(31, 'step/start', { turn: 4, step: 1 }),
+      at(32, 'assistant/message', {
+        turn: 4,
+        step: 1,
+        message: {
+          ...assistantMessage('assistant-malformed', ''),
+          content: [{ type: 'reasoning', text: '<tool_call>invalid</tool_call>' }],
+        },
+      }, { surfaceOp: 'append' }),
+      at(33, 'llm/retry', {
+        retryId: 'retry-semantic',
+        turn: 4,
+        step: 1,
+        provider: 'fake',
+        mode: 'normal',
+        policyKey: 'fake-normal',
+        retry: 1,
+        maxRetries: 2,
+        delayMs: 10,
+        failure: { code: 'MALFORMED_TOOL_CALL', message: 'no tool was executed' },
+      }),
+      at(34, 'llm/retry-started', { retryId: 'retry-semantic', turn: 4, step: 1, retry: 1 }),
+      at(35, 'user/message', {
+        ...textMessage('malformed-reminder', '<system-reminder>No tool was executed.</system-reminder>'),
+        source: { kind: 'plugin', plugin: 'malformed-tool-call-retry' },
+      }, { surfaceOp: 'append' }),
+      at(36, 'step/end', { turn: 4, step: 1 }),
+      at(37, 'step/start', { turn: 4, step: 2 }),
+      at(38, 'assistant/message', {
+        turn: 4,
+        step: 2,
+        message: assistantMessage('assistant-recovered', 'recovered'),
+      }, { surfaceOp: 'append' }),
+      at(39, 'step/end', { turn: 4, step: 2 }),
+      at(40, 'turn/end', { turn: 4, reason: { kind: 'completed' } }),
+    ])
+    const semanticAssistants = [...snapshot(semanticRetryValue).nodes.values()]
+      .filter(candidate => candidate.kind === 'assistant-step')
+    expect(semanticAssistants).toHaveLength(2)
+    expect(semanticAssistants.map(candidate => (candidate.data as AssistantChatData).blocks))
+      .toEqual([
+        [{ kind: 'reasoning', text: '<tool_call>invalid</tool_call>' }],
+        [{ kind: 'text', text: 'recovered' }],
+      ])
+    const loadedSemanticRetry = assembler([
+      at(32, 'assistant/message', {
+        turn: 4,
+        step: 1,
+        message: {
+          ...assistantMessage('assistant-malformed', ''),
+          content: [{ type: 'reasoning', text: '<tool_call>invalid</tool_call>' }],
+        },
+      }, { surfaceOp: 'append' }),
+      at(33, 'llm/retry', {
+        retryId: 'retry-semantic',
+        turn: 4,
+        step: 1,
+        provider: 'fake',
+        mode: 'normal',
+        policyKey: 'fake-normal',
+        retry: 1,
+        maxRetries: 2,
+        delayMs: 10,
+        failure: { code: 'MALFORMED_TOOL_CALL', message: 'no tool was executed' },
+      }),
+      at(36, 'step/end', { turn: 4, step: 1 }),
+    ], true)
+    expect((node(snapshot(loadedSemanticRetry), 'assistant-step')?.data as AssistantChatData).blocks)
+      .toEqual([{ kind: 'reasoning', text: '<tool_call>invalid</tool_call>' }])
+
     const toolOnlyValue = assembler([
       at(30, 'turn/start', { turn: 4 }),
       at(31, 'step/start', { turn: 4, step: 1 }),
diff --git a/packages/core/agent-loop/README.i18n.yaml b/packages/core/agent-loop/README.i18n.yaml
index 30a2ca2ebd..9706c8c940 100644
--- a/packages/core/agent-loop/README.i18n.yaml
+++ b/packages/core/agent-loop/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/core/agent-loop/README.md
-README.md: c9fd1d15a806874f85da604c3db90c10aa8c78d1
-README.zh.md: d8e2a34e97871e531a2c7c2da7ba47ae8e8c3cf9
+README.md: f1b5519672a6fdb226b2bfedfdc27ce41938f3e9
+README.zh.md: fac234994920ad979890c883c85472872cc02c77
diff --git a/packages/core/agent-loop/README.md b/packages/core/agent-loop/README.md
index c9fd1d15a8..f1b5519672 100644
--- a/packages/core/agent-loop/README.md
+++ b/packages/core/agent-loop/README.md
@@ -63,13 +63,13 @@ Every inbox mutation publishes one normalized `agent/inbox/spliced` event before
 
 The driver owns one agent for its lifetime and runs inside `ctx.agents.withInitiator(agent, ...)`. Package-private orchestration entry points recover the exact Agent, derive `agent.session` once, and let operation-local helpers capture it instead of forwarding the concrete driver or per-operation `Session` through shallow interfaces. A helper keeps an explicit `Session` when that is its actual interface, while creation, persistence load, unpublished setup, services, workers, processes, persistence, and wire protocols retain their explicit identities. The [agent service](../agent/README.md#initiating-agent-scope) owns propagation, teardown, and detached-work rules.
 
-Every provider call that reaches a successful finish appends exactly one `assistant/message` completion anchor, including content-less calls and `max-tokens` finishes, except a reasoning-only stop containing tool-call markup that enters corrective recovery. The anchor records the assembled content as-is, lists the exact chunk seqs in `sourceEventSeqs` (`[]` for a stream with no chunks), and includes usage when available; empty content stays out of derived message history. A turn cancellation that interrupts streaming also appends an `interrupted: true` anchor when non-empty text or reasoning has reached the user. The anchor cites those chunk seqs and places the rendered prefix in derived message history, so the next request contains what the user saw. Undispatched tool calls are omitted, and an empty or tool-only stream produces no anchor; provider failures still commit no assistant content ([decision](../../../.agents/notes/implemented/architecture/2026-08-10-cancelled-stream-prefix-finalize.md)).
+Every provider call that reaches a successful finish appends exactly one `assistant/message` completion anchor, including content-less calls, `max-tokens` finishes, and a reasoning-only malformed-tool attempt retained for correction. The anchor records the assembled content as-is, lists the exact chunk seqs in `sourceEventSeqs` (`[]` for a stream with no chunks), and includes usage when available; empty content stays out of derived message history. A malformed attempt deliberately omits adapter-private replay state so its durable reasoning is replayed as provider-neutral assistant history rather than trusted as a valid native response. A turn cancellation that interrupts streaming also appends an `interrupted: true` anchor when non-empty text or reasoning has reached the user. The anchor cites those chunk seqs and places the rendered prefix in derived message history, so the next request contains what the user saw. Undispatched tool calls are omitted, and an empty or tool-only stream produces no anchor; provider failures still commit no assistant content ([decision](../../../.agents/notes/implemented/architecture/2026-08-10-cancelled-stream-prefix-finalize.md)).
 
 After `agent/request` returns a provider/model call config, the loop asks `ctx.llm.prepareCall()` to validate adapter-owned fields and materialize configured reasoning-effort and output-token defaults under the active turn signal. The prepared call retains the exact adapter registration across this asynchronous resolution, `request/header` logging, and terminal dispatch, so HMR cannot mix one adapter's capability result with another adapter's request. The header records the effective config and which fields came from the adapter. Before the next waterfall, the loop removes those marked fields from the proposal so the current exact route rematerializes its own defaults; unmarked explicit settings persist across steps and route changes. A route with no registered adapter preserves the proposed config so an `llm/stream` listener can own and short-circuit it; unhandled terminal dispatch still fails with `NO_ADAPTER`. A new loop instance follows the same adapter-default marker rule when resuming.
 
 Plugin failure ends the current turn, not the loop. Final adapter selection, dispatch, and iteration failures arrive from `ctx.llm` as terminal error or aborted finishes and enter `agent/request-error`; middleware, result processing, tools, and other extension failures remain thrown and close directly. Recovery receives request coordinates, immutable provider facts, the immutable retry policy captured by the prepared adapter registration, and the turn signal; the policy is absent when middleware owns an unprepared route. A handling listener returns `{ kind: 'retry' }`; an unhandled failure is terminal. AgentLoop owns one cancellation signal for the current admission or turn. An effective `cancel(cause)` clears pending work unless `keepInbox` is set and cooperatively aborts that signal; idle cancellation is a no-op. Waking input that lands after the abort fires but before the activity converges to idle is latched (`wakeRequested`) and replayed at the driver's own convergence boundary, so it runs without a further waking send; a `disposed` cancel never latches, and a wake submitted while already idle always opens its turn boundary (status shows a transient `idle → running → idle` pair even when the message was cleared). Durable `turn/end` records `aborted` for `user` and `parent`, while disposal records `disposed`; undispatched model tool calls receive synthetic `tool/call` and `ABORTED_BEFORE_DISPATCH` result pairs. The cancellation cause changes reporting, not how result context finalized after cancellation is handled. Disposal waits for signal-ignoring work before registry removal. The [explicit-cancellation decision](../../../.agents/notes/implemented/architecture/2026-07-16-explicit-turn-cancellation.md) and the [cancel-convergence wake latch](../../../.agents/notes/implemented/bug-fix/2026-08-07-cancel-convergence-wake-latch.md) own the lifecycle and race contract.
 
-A normal `stop` whose only non-empty output is private reasoning containing Qwen-style tool-call markup is a `MALFORMED_TOOL_CALL`, never an executable call. The first defect enters `agent/request-error`; an accepted retry receives a transient `<system-reminder>` requiring the native structured tool channel when a tool is needed and otherwise allowing continued reasoning or a final answer. The loop appends that reminder as one request-local user-role message after the exact durable message prefix, while leaving the system prompt and request header unchanged. It is not persisted into history or later steps, and request reconstruction accepts only this exact package-owned suffix. A repeated reasoning-only result fails immediately without another recovery offer. Any visible text or native output block keeps quoted examples inert and follows the normal completion path.
+A normal `stop` whose only non-empty output is private reasoning containing Qwen-style tool-call markup is a `MALFORMED_TOOL_CALL`, never an executable call. The invalid assistant attempt is retained verbatim. The first defect enters `agent/request-error`; after an accepted retry, the loop durably appends one user-role `<system-reminder>` immediately after that attempt and runs the corrective request in a new step over the same prompt assembly. The notice says that no tool was executed, requires the native tool channel when a tool is needed, and otherwise allows continued reasoning or a final answer. The failed attempt and reminder therefore remain in later derived history, while the system prompt and request header stay unchanged and request reconstruction needs no unlogged exception. A repeated reasoning-only result is also retained, then fails immediately without another recovery offer. Any visible text or native output block keeps quoted examples inert and follows the normal completion path.
 
 Within a step, exclusive calls form barriers; parallel-safe calls use a bounded rolling pool and are reclassified before start. Only dispatch/body overlaps. Policy, durable results, and result context remain model-ordered. Abort stops new calls, drains started results, and retains their finalized result context without distinguishing the cancellation cause. An internal scheduler failure stops new dispatches, waits for already-started dispatches, and reaches the turn error boundary without fabricating tool results.
 
@@ -90,7 +90,7 @@ Everything that goes beyond "call the model, run the tools, repeat" belongs to p
 
 #### What the model sees
 
-For each step, the loop sends the rendered per-agent system prompt, visible tool schemas, and the session's derived messages. It supplies `provider`, `model`, and `cwd` variable values. Only a one-time retry after `MALFORMED_TOOL_CALL` appends a transient request-tail user message containing a `<system-reminder>` that requires a native structured call when a tool is needed, forbids tool-call markup in reasoning or visible text, and otherwise permits continued reasoning or a final answer. The base system prompt remains byte-identical.
+For each step, the loop sends the rendered per-agent system prompt, visible tool schemas, and the session's derived messages. It supplies `provider`, `model`, and `cwd` variable values. A one-time retry after `MALFORMED_TOOL_CALL` retains the invalid assistant reasoning and follows it with one durable user-role `<system-reminder>` saying that no tool was executed. The reminder requires the native tool channel when a tool is needed, forbids tool-call markup in reasoning or visible text, and otherwise permits continued reasoning or a final answer. Both messages remain in later requests; the base system prompt remains byte-identical.
 
 #### Token effect
 
@@ -98,7 +98,7 @@ System text and schemas are paid again on every step. Per-agent scoping chooses
 
 #### KV Cache effect
 
-Append-only only while system text, schemas, and earlier history remain byte-identical under the same provider and model route. A token-bearing assembly rewrite or composition change may invalidate reuse from the first altered request token. The malformed-tool corrective reminder instead preserves the entire original system/tools/history prefix and adds tokens only at the request tail, so providers can reuse the prior prefix cache; the tail affects only that retry.
+Append-only only while system text, schemas, and earlier history remain byte-identical under the same provider and model route. A token-bearing assembly rewrite or composition change may invalidate reuse from the first altered request token. Malformed-tool recovery preserves the entire original system/tools/history prefix and appends the failed assistant reasoning plus its corrective reminder. Providers can therefore reuse the prior prefix cache, and later requests can reuse this longer durable prefix instead of paying for duplicate reminders.
 
 ### Retained message history
 
diff --git a/packages/core/agent-loop/README.zh.md b/packages/core/agent-loop/README.zh.md
index d8e2a34e97..fac2349949 100644
--- a/packages/core/agent-loop/README.zh.md
+++ b/packages/core/agent-loop/README.zh.md
@@ -63,13 +63,13 @@ interface Config {
 
 驱动器在其整个生命周期内拥有一个 agent，并在 `ctx.agents.withInitiator(agent, ...)` 内运行。包私有的编排入口点会恢复确切的 Agent，一次性派生 `agent.session`，并让操作局部的辅助函数捕获它，而不是通过浅层接口继续传递具体驱动器或每次操作的 `Session`。如果显式 `Session` 正是辅助函数的实际接口，该辅助函数会保留它；创建、持久化加载、未发布 setup、服务、worker、进程、持久化和 wire 协议则继续保留各自的显式身份。[agent 服务](../agent/README.zh.md#initiating-agent-scope)规定传播、teardown 和分离工作规则。
 
-每次提供方调用成功结束时，都会恰好追加一个 `assistant/message` 完成锚点，包括无内容调用和以 `max-tokens` 结束的调用；唯一例外是只含 reasoning 且带有工具调用标记、已进入纠错恢复的 stop。该锚点原样记录组装后的内容，在 `sourceEventSeqs` 中列出确切的分片 seq（流没有分片时为 `[]`），并在用量可用时包含用量；空内容不会进入派生消息历史。轮次取消打断流式输出时，如果非空文本或推理内容已送达用户，循环也会追加一个带 `interrupted: true` 的锚点。该锚点引用对应的分片 seq，并把已渲染的前缀放入派生消息历史，使下一次请求包含用户看到的内容。未分派的工具调用会被省略，空流或只包含工具调用的流不会生成锚点；提供方故障也不提交 assistant 内容（[决策](../../../.agents/notes/implemented/architecture/2026-08-10-cancelled-stream-prefix-finalize.zh.md)）。
+每次提供方调用成功结束时，都会恰好追加一个 `assistant/message` 完成锚点，包括无内容调用、以 `max-tokens` 结束的调用，以及为纠错而保留的 reasoning-only 工具格式异常尝试。该锚点原样记录组装后的内容，在 `sourceEventSeqs` 中列出确切的分片 seq（流没有分片时为 `[]`），并在用量可用时包含用量；空内容不会进入派生消息历史。格式异常尝试会有意省略适配器私有 replay state，使其持久 reasoning 作为提供方无关的 assistant 历史重放，而不再被当成有效原生响应信任。轮次取消打断流式输出时，如果非空文本或推理内容已送达用户，循环也会追加一个带 `interrupted: true` 的锚点。该锚点引用对应的分片 seq，并把已渲染的前缀放入派生消息历史，使下一次请求包含用户看到的内容。未分派的工具调用会被省略，空流或只包含工具调用的流不会生成锚点；提供方故障也不提交 assistant 内容（[决策](../../../.agents/notes/implemented/architecture/2026-08-10-cancelled-stream-prefix-finalize.zh.md)）。
 
 在 `agent/request` 返回提供方／模型调用配置后，循环会调用 `ctx.llm.prepareCall()`，在活跃轮次信号的控制下校验由适配器负责的字段，并填入配置的推理（reasoning）强度和输出 token 默认值。准备完成的调用会在这次异步解析、`request/header` 日志记录和最终分派期间保留同一项确切的适配器注册，因此 HMR（热模块替换）不会把某个适配器的能力解析结果与另一适配器的请求混用。请求 header 会记录生效配置以及哪些字段来自适配器。下一次 waterfall（瀑布式事件）前，循环会从提议中移除这些带标记字段，使当前精确路由重新填入自身默认值；未带标记的显式设置会跨步骤和路由变化保留。没有已注册适配器的路由会保留原定配置，使 `llm/stream` 监听器可以接管并短路该请求；最终分派仍会以 `NO_ADAPTER` 拒绝未得到处理的路由。新循环实例在恢复时会遵循同一套适配器默认值标记规则。
 
 插件失败会结束当前轮次，而不是结束循环。最终适配器选择、分发与迭代失败会以终止错误或中止结束的形式由 `ctx.llm` 传来，并进入 `agent/request-error`；middleware、结果处理、工具及其他扩展失败仍会抛出并直接关闭轮次。恢复逻辑会接收请求坐标、不可变的提供方事实、准备完成的适配器注册所捕获的不可变重试策略以及轮次信号；middleware 接管未准备路由时，该策略缺失。处理失败的监听器返回 `{ kind: 'retry' }`；未被处理的失败是终态。AgentLoop 为当前准入操作或轮次拥有一个取消信号。有效的 `cancel(cause)` 在未设置 `keepInbox` 时清除待处理工作，并以协作方式中止该信号；空闲取消是空操作。abort 触发后、活动收敛到空闲前到达的唤醒输入会被锁存（`wakeRequested`），并在 driver 自身的收敛边界重放，无需再发一条唤醒 send 即可执行；`disposed` 取消从不锁存，而 agent 已处于空闲时发送的唤醒总是打开自己的 turn 边界（即使消息已被清除，状态也会显示瞬态 `idle → running → idle` 对）。持久 `turn/end` 为 `user` 和 `parent` 记录 `aborted`，dispose 则记录 `disposed`；未分发的模型工具调用会收到合成的 `tool/call` 与 `ABORTED_BEFORE_DISPATCH` 结果对。取消原因只影响报告方式，不影响如何处理在取消后完成终结的结果上下文。dispose 会等待忽略信号的工作完成，然后才从注册表移除。[显式取消决策](../../../.agents/notes/implemented/architecture/2026-07-16-explicit-turn-cancellation.zh.md)与[取消收敛窗口唤醒锁存](../../../.agents/notes/implemented/bug-fix/2026-08-07-cancel-convergence-wake-latch.zh.md)规定生命周期与竞态约定。
 
-如果普通 `stop` 的唯一非空输出是私有 reasoning，且其中包含 Qwen 风格工具调用标记，则它会成为 `MALFORMED_TOOL_CALL`，绝不会被当作可执行调用。首次缺陷进入 `agent/request-error`；恢复被接受后，只为该次重试追加临时 `<system-reminder>`，要求需要工具时使用原生结构化通道，不需要工具时可以继续思考或给出最终回答。循环会把该 reminder 作为一条仅用于本次请求的 user-role 尾消息，追加在完全不变的持久消息前缀之后；系统提示词与请求 header 保持不变。它不写入历史，也不会进入后续步骤，请求重建校验只放行这个由包自身生成的精确尾缀。若再次只返回 reasoning，则立即失败，不再提供恢复机会。只要存在可见文本或原生输出块，引用示例就保持惰性，并走正常完成路径。
+如果普通 `stop` 的唯一非空输出是私有 reasoning，且其中包含 Qwen 风格工具调用标记，则它会成为 `MALFORMED_TOOL_CALL`，绝不会被当作可执行调用。无效 assistant 尝试会原样保留。首次缺陷进入 `agent/request-error`；恢复被接受后，循环会立即在该尝试之后持久追加一条 user-role `<system-reminder>`，并在复用同一份 prompt assembly 的新步骤中执行纠错请求。这条通知明确说明没有工具被执行，要求需要工具时使用原生工具通道，不需要工具时可以继续思考或给出最终回答。因此，失败尝试与 reminder 会继续保留在后续派生历史中，而系统提示词和请求 header 保持不变，请求重建也不再需要未记录尾缀的例外。若再次只返回 reasoning，该结果同样会被保留，随后立即失败且不再提供恢复机会。只要存在可见文本或原生输出块，引用示例就保持惰性，并走正常完成路径。
 
 在步骤内，独占调用形成屏障；并行安全调用使用有界滚动池，并在启动前重新分类。只有分发和调用主体的执行会发生重叠。策略、持久结果和结果上下文仍保持模型顺序。中止会阻止启动新的调用，等待已启动调用的结果处理完毕，并保留其完成终结后的结果上下文，不区分取消原因。内部调度器故障会停止新的分发，等待已启动的分发，然后在不虚构工具结果的情况下到达轮次错误边界。
 
@@ -90,7 +90,7 @@ interface Config {
 
 #### 模型看到的内容
 
-每个步骤中，循环会发送针对该 agent 呈现的系统提示词、可见工具 schema 和会话派生消息。它提供 `provider`、`model` 与 `cwd` 变量值。只有 `MALFORMED_TOOL_CALL` 后的一次重试会在请求尾部追加一条临时 user 消息，其中包含 `<system-reminder>`：需要工具时必须发原生结构化调用，不得在 reasoning 或可见文本里写工具调用标记；否则可以继续思考或给出最终回答。基础 system prompt 保持逐字节不变。
+每个步骤中，循环会发送针对该 agent 呈现的系统提示词、可见工具 schema 和会话派生消息。它提供 `provider`、`model` 与 `cwd` 变量值。`MALFORMED_TOOL_CALL` 后的一次重试会保留无效 assistant reasoning，并紧接着持久添加一条 user-role `<system-reminder>`，明确说明没有工具被执行。该 reminder 要求需要工具时使用原生工具通道，不得在 reasoning 或可见文本里写工具调用标记；否则可以继续思考或给出最终回答。两条消息都会保留在后续请求中；基础 system prompt 保持逐字节不变。
 
 #### Token 影响
 
@@ -98,7 +98,7 @@ interface Config {
 
 #### KV Cache 影响
 
-只有在同一提供方和模型路由下，且系统文本、schema 与此前历史都保持逐字节一致时，请求 token 序列才保持仅追加。携带 token 的组装改写或组合变更可能从第一个改变的请求 token 起使复用失效。工具格式纠错 reminder 则保留原 system、tools 与历史组成的完整前缀，只在请求尾部增加 token，因此提供方仍可复用此前的前缀缓存；新增尾部只影响对应重试。
+只有在同一提供方和模型路由下，且系统文本、schema 与此前历史都保持逐字节一致时，请求 token 序列才保持仅追加。携带 token 的组装改写或组合变更可能从第一个改变的请求 token 起使复用失效。工具格式纠错会保留原 system、tools 与历史组成的完整前缀，并在尾部追加失败 assistant reasoning 及纠错 reminder。因此提供方仍可复用此前的前缀缓存，后续请求也可以复用这段更长的持久前缀，而不需要重复添加 reminder。
 
 ### 保留的消息历史
 
diff --git a/packages/core/agent-loop/src/agent.ts b/packages/core/agent-loop/src/agent.ts
index 317b47f2ee..4e0a852045 100644
--- a/packages/core/agent-loop/src/agent.ts
+++ b/packages/core/agent-loop/src/agent.ts
@@ -34,7 +34,7 @@ import { joinContextSections, renderContextSections, renderPrompt } from '@deeps
 import type { PromptAssembly } from '@deepseek-ai/dsh-system-prompt'
 import type { Context } from '@deepseek-ai/cordis'
 import { RuntimeContextProjection } from './runtime-context.ts'
-import { appendMalformedToolCallRetryContext } from './malformed-tool-call.ts'
+import { createMalformedToolCallRetryReminder } from './malformed-tool-call.ts'
 import { executeToolCalls } from './tool-calls.ts'
 
 type Phase =
@@ -49,6 +49,8 @@ type Phase =
 
 type StepEndReason = Extract<TurnEndReason, { kind: 'completed' | 'max-tokens' }>
 
+type StepResult = StepEndReason | { kind: 'malformed-tool-call-retry' } | null
+
 type PreparedStep =
   | { kind: 'reject' }
   | { kind: 'enter'; messages: UserMessage[]; assembly: PromptAssembly }
@@ -282,11 +284,16 @@ export class ReactLoopAgent implements Agent {
     phase.turn = turn
     let turnEnds: TurnEndReason | null = null
     let target: InboxTarget = 'next-turn'
+    let retryAssembly: PromptAssembly | undefined
+    let malformedToolCallRetry = false
     try {
       while (true) {
         signal.throwIfAborted()
         const step = phase.step + 1
-        const decision = await this.preStep(target, { turn, step })
+        const decision: PreparedStep = retryAssembly === undefined
+          ? await this.preStep(target, { turn, step })
+          : { kind: 'enter', messages: [], assembly: retryAssembly }
+        retryAssembly = undefined
         if (decision.kind === 'reject') {
           turnEnds = { kind: 'blocked' }
           return false
@@ -301,19 +308,33 @@ export class ReactLoopAgent implements Agent {
         signal.throwIfAborted()
         this.session.append('step/start', { turn, step })
         phase.step = step
+        let stepEnd: StepResult
         try {
           for (const message of decision.messages) {
             this.session.append('user/message', message, { surfaceOp: 'append' })
           }
           // max-tokens is sticky: once any step hits the ceiling, later steps
           // that complete normally must not downgrade the turn outcome.
-          const stepEnd = await this.step(decision.assembly)
+          stepEnd = await this.step(decision.assembly, malformedToolCallRetry)
+          malformedToolCallRetry = false
           // max-tokens stays sticky: a later completed step must not
           // downgrade the turn outcome.
-          if (turnEnds === null || turnEnds.kind !== 'max-tokens') turnEnds = stepEnd
+          if (stepEnd?.kind !== 'malformed-tool-call-retry'
+            && (turnEnds === null || turnEnds.kind !== 'max-tokens')) {
+            turnEnds = stepEnd
+          }
         } finally {
           this.session.append('step/end', { turn, step })
         }
+        if (stepEnd?.kind === 'malformed-tool-call-retry') {
+          // A normal finish produced a durable assistant message, so its
+          // corrective attempt owns a new step instead of visually replacing
+          // the failed Think. Reuse the exact same prompt assembly and add no
+          // new inbox or runtime-context messages at this internal boundary.
+          retryAssembly = decision.assembly
+          malformedToolCallRetry = true
+          continue
+        }
         signal.throwIfAborted()
         if (turnEnds && this.inbox.nextStep.length === 0) {
           await this.dispatch.serial('agent/turn-stopping', { turn, signal })
@@ -352,13 +373,15 @@ export class ReactLoopAgent implements Agent {
     return true
   }
 
-  private async step(assembly: PromptAssembly): Promise<StepEndReason | null> {
+  private async step(
+    assembly: PromptAssembly,
+    malformedToolCallRetry: boolean,
+  ): Promise<StepResult> {
     /* v8 ignore next -- private callers establish the running phase before executing a step */
     if (this.phase.kind !== 'running') throw new Error(`agent "${this.id}": step outside running phase`)
     const { turn, step, abort: { signal } } = this.phase
     signal.throwIfAborted()
     const system = renderPrompt(assembly)
-    let malformedToolCallRetry = false
 
     while (true) {
       const boundaryMessages = this.session.deriveMessages()
@@ -367,9 +390,7 @@ export class ReactLoopAgent implements Agent {
         step,
         assembly.tools,
         system,
-        malformedToolCallRetry
-          ? appendMalformedToolCallRetryContext(boundaryMessages)
-          : boundaryMessages,
+        boundaryMessages,
         signal,
       )
       const assembler = new BlockAssembler()
@@ -423,13 +444,38 @@ export class ReactLoopAgent implements Agent {
 
       const content = assembler.blocks()
       const reasoningOnly = finish.kind === 'stop' ? reasoningOnlyText(content) : undefined
-      if (reasoningOnly !== undefined
-        && (malformedToolCallRetry || TOOL_CALL_MARKUP.test(reasoningOnly))) {
-        const repeatedWithoutMarkup = malformedToolCallRetry && !TOOL_CALL_MARKUP.test(reasoningOnly)
+      const malformedReasoning = reasoningOnly !== undefined
+        && (malformedToolCallRetry || TOOL_CALL_MARKUP.test(reasoningOnly))
+      const message = createAssistantMessage({
+        content,
+        source: {
+          provider: request.provider,
+          model: request.model,
+          // The durable blocks remain authoritative, but provider-native replay
+          // metadata from an invalid attempt must not make an adapter trust or
+          // suppress the malformed reasoning on the corrective request.
+          ...!malformedReasoning && assembler.replayState !== undefined
+            ? { replayState: assembler.replayState }
+            : {},
+        },
+      })
+      this.session.append(
+        'assistant/message',
+        {
+          turn,
+          step,
+          message,
+          ...assembler.usage === undefined ? {} : { usage: assembler.usage },
+        },
+        { surfaceOp: 'append', sourceEventSeqs: chunkSeqs },
+      )
+      if (malformedReasoning) {
+        const repeatedWithoutMarkup = malformedToolCallRetry
+          && !TOOL_CALL_MARKUP.test(reasoningOnly)
         const error = new LlmError(
           repeatedWithoutMarkup
-            ? `model "${request.provider}/${request.model}" still returned only private reasoning after malformed tool-call recovery`
-            : `model "${request.provider}/${request.model}" wrote tool-call markup in reasoning instead of emitting a structured tool call`,
+            ? `model "${request.provider}/${request.model}" still returned only private reasoning after malformed tool-call recovery; no tool was executed`
+            : `model "${request.provider}/${request.model}" wrote tool-call markup in reasoning; no tool was executed`,
           MALFORMED_TOOL_CALL_CODE,
         )
         if (!malformedToolCallRetry) {
@@ -446,30 +492,15 @@ export class ReactLoopAgent implements Agent {
           )
           signal.throwIfAborted()
           if (action?.kind !== 'retry') throw error
-          malformedToolCallRetry = true
-          continue
+          this.session.append(
+            'user/message',
+            createMalformedToolCallRetryReminder(),
+            { surfaceOp: 'append' },
+          )
+          return { kind: 'malformed-tool-call-retry' }
         }
         throw error
       }
-
-      const message = createAssistantMessage({
-        content,
-        source: {
-          provider: request.provider,
-          model: request.model,
-          ...assembler.replayState !== undefined ? { replayState: assembler.replayState } : {},
-        },
-      })
-      this.session.append(
-        'assistant/message',
-        {
-          turn,
-          step,
-          message,
-          ...assembler.usage === undefined ? {} : { usage: assembler.usage },
-        },
-        { surfaceOp: 'append', sourceEventSeqs: chunkSeqs },
-      )
       if (finish.kind === 'max-tokens') return { kind: 'max-tokens' }
 
       const toolCalls = message.content.filter(block => block.type === 'tool-call')
diff --git a/packages/core/agent-loop/src/invariant.ts b/packages/core/agent-loop/src/invariant.ts
index 3b3db64ee2..80fdfba8f9 100644
--- a/packages/core/agent-loop/src/invariant.ts
+++ b/packages/core/agent-loop/src/invariant.ts
@@ -7,7 +7,6 @@ import type { Context } from '@deepseek-ai/cordis'
 import { isAgentLoopRequest, type GenerateOptions } from '@deepseek-ai/dsh-llm'
 import type { InvariantFailure, InvariantInstaller } from '@deepseek-ai/dsh-invariants'
 import { foldRequestHeader } from '@deepseek-ai/dsh-session'
-import { isMalformedToolCallRetryContext } from './malformed-tool-call.ts'
 
 const PACKAGE_NAME = '@deepseek-ai/dsh-agent-loop'
 
@@ -38,8 +37,7 @@ const install: InvariantInstaller = Object.assign((ctx: Context, fail: Invariant
       return fail('a loop-built request with no request/header event in its session log')
     }
     const expected = session.deriveMessages()
-    if (JSON.stringify(options.messages) !== JSON.stringify(expected)
-      && !isMalformedToolCallRetryContext(options.messages, expected)) {
+    if (JSON.stringify(options.messages) !== JSON.stringify(expected)) {
       fail(`llm request for session "${String(session.id)}" diverges from the dispatch-time durable derivation (log-reconstruction desync)`)
     }
 
diff --git a/packages/core/agent-loop/src/malformed-tool-call.ts b/packages/core/agent-loop/src/malformed-tool-call.ts
index fb0d000925..79539a2b74 100644
--- a/packages/core/agent-loop/src/malformed-tool-call.ts
+++ b/packages/core/agent-loop/src/malformed-tool-call.ts
@@ -1,45 +1,26 @@
-/** Request-local recovery context for malformed reasoning tool calls. */
+/** Durable recovery context for malformed reasoning tool calls. */
 
 import { createUserMessage } from '@deepseek-ai/dsh-llm'
-import type { Message } from '@deepseek-ai/dsh-llm'
+import type { UserMessage } from '@deepseek-ai/dsh-llm'
 
-/** One-request guidance used only after a model wrote a would-be tool call in reasoning. */
+/** Guidance retained immediately after a model wrote a would-be tool call in reasoning. */
 export const MALFORMED_TOOL_CALL_RETRY_REMINDER = `<system-reminder>
-Your previous attempt ended with private reasoning but no final answer or structured tool call. Retry the same task now.
-When a tool is needed, invoke it only through the native structured tool-call channel. Do not write tool-call XML, JSON, tags, or arguments in reasoning or visible text, and do not merely describe the call.
+The immediately preceding assistant attempt was invalid, but it has been retained in the conversation.
+No tool was executed for that attempt. Retry the same task now.
+When a tool is needed, invoke it only through the native tool-call channel. Do not write tool-call XML, JSON, tags, or arguments in reasoning or visible text, and do not merely describe the call.
 If no tool is needed, continue reasoning or provide the final answer.
 </system-reminder>`
 
 const RETRY_CONTEXT_SOURCE = '@deepseek-ai/dsh-agent-loop/malformed-tool-call-retry'
 
 /**
- * Append recovery guidance without changing the durable history or request header.
- * Keeping the original messages as an exact prefix preserves provider prompt-cache reuse.
+ * Create the durable user-role notice placed immediately after the invalid
+ * assistant attempt. The familiar frame marks plugin-owned guidance without
+ * pretending that pi-ai carries an in-history system role.
  */
-export function appendMalformedToolCallRetryContext(messages: readonly Message[]): Message[] {
-  return [...messages, createUserMessage({
+export function createMalformedToolCallRetryReminder(): UserMessage {
+  return createUserMessage({
     content: [{ type: 'text', text: MALFORMED_TOOL_CALL_RETRY_REMINDER }],
     source: { kind: 'plugin', plugin: RETRY_CONTEXT_SOURCE },
-  })]
-}
-
-/**
- * Accept only the package-owned, exact one-message suffix as a legal deviation
- * from durable request reconstruction.
- */
-export function isMalformedToolCallRetryContext(
-  messages: readonly Message[],
-  durableMessages: readonly Message[],
-): boolean {
-  if (messages.length !== durableMessages.length + 1) return false
-  if (JSON.stringify(messages.slice(0, -1)) !== JSON.stringify(durableMessages)) return false
-  // Invariant callers may hand us an intentionally malformed runtime value.
-  const tail = messages.at(-1) as Partial<Message> | undefined
-  if (tail?.role !== 'user' || tail.source?.kind !== 'plugin') return false
-  return Object.keys(tail).sort().join(',') === 'content,id,role,source'
-    && Object.keys(tail.source).sort().join(',') === 'kind,plugin'
-    && tail.source.plugin === RETRY_CONTEXT_SOURCE
-    && JSON.stringify(tail.content) === JSON.stringify([{
-      type: 'text', text: MALFORMED_TOOL_CALL_RETRY_REMINDER,
-    }])
+  })
 }
diff --git a/packages/core/agent-loop/tests/invariant.spec.ts b/packages/core/agent-loop/tests/invariant.spec.ts
index 775fdfa509..5809476343 100644
--- a/packages/core/agent-loop/tests/invariant.spec.ts
+++ b/packages/core/agent-loop/tests/invariant.spec.ts
@@ -4,7 +4,7 @@ import SessionStore, { SessionId } from '@deepseek-ai/dsh-session'
 import InvariantRegistry from '@deepseek-ai/dsh-invariants'
 import * as AgentLoopInvariant from '@deepseek-ai/dsh-agent-loop/invariant'
 import { createUserMessage, markAgentLoopRequest, type GenerateOptions  } from '@deepseek-ai/dsh-llm'
-import { appendMalformedToolCallRetryContext } from '../src/malformed-tool-call.ts'
+import { createMalformedToolCallRetryReminder } from '../src/malformed-tool-call.ts'
 
 async function setup(): Promise<Context> {
   const ctx = new Context()
@@ -67,20 +67,17 @@ describe('request-reconstruction invariant', () => {
       .toThrow(/diverges from the dispatch-time durable derivation/)
   })
 
-  it('allows only the exact package-owned malformed-tool retry suffix', async () => {
+  it('requires malformed-tool recovery guidance to enter durable history before dispatch', async () => {
     const { ctx, session, boundary } = await requestSetup()
-    const recovery = Object.freeze(appendMalformedToolCallRetryContext(boundary))
-    expect(() => { dispatch(ctx, loopRequest({ model: 'm', messages: recovery, sessionId: session.id })) })
-      .not.toThrow()
-    expect(session.deriveMessages()).toEqual(boundary)
-
-    const tail = recovery.at(-1)!
-    const wrongSource = Object.freeze([
-      ...boundary,
-      Object.freeze({ ...tail, source: { kind: 'plugin' as const, plugin: 'untrusted' } }),
-    ])
-    expect(() => { dispatch(ctx, loopRequest({ model: 'm', messages: wrongSource, sessionId: session.id })) })
+    const reminder = createMalformedToolCallRetryReminder()
+    const unlogged = Object.freeze([...boundary, reminder])
+    expect(() => { dispatch(ctx, loopRequest({ model: 'm', messages: unlogged, sessionId: session.id })) })
       .toThrow(/diverges from the dispatch-time durable derivation/)
+
+    session.append('user/message', reminder, { surfaceOp: 'append' })
+    const durable = Object.freeze(session.deriveMessages())
+    expect(() => { dispatch(ctx, loopRequest({ model: 'm', messages: durable, sessionId: session.id })) })
+      .not.toThrow()
   })
 
   it('rejects message and header divergence', async () => {
diff --git a/packages/core/agent-loop/tests/loop.spec.ts b/packages/core/agent-loop/tests/loop.spec.ts
index 87c45e7ea5..72e88da89a 100644
--- a/packages/core/agent-loop/tests/loop.spec.ts
+++ b/packages/core/agent-loop/tests/loop.spec.ts
@@ -234,8 +234,15 @@ describe('agent loop', () => {
 
   it('retries reasoning-only pseudo tool markup once and accepts a native tool call', async () => {
     const pseudoCall = '<tool_call><function=echo><parameter=text>ping</parameter></function></tool_call>'
+    const malformed = reasoningResponse(`I should use the tool.\n${pseudoCall}`)
+    const malformedFinish = malformed.at(-1)
+    if (malformedFinish?.type !== 'finish') throw new Error('reasoning response must end with finish')
+    malformedFinish.replayState = {
+      response: { kind: 'mock-native', version: 1 },
+      blocks: [{ type: 'reasoning', native: true }],
+    }
     const adapter = new MockAdapter([
-      reasoningResponse(`I should use the tool.\n${pseudoCall}`),
+      malformed,
       toolCallResponse('c1', 'echo', { text: 'ping' }),
       textResponse('done'),
     ])
@@ -267,7 +274,14 @@ describe('agent loop', () => {
     const nextStepRequest = adapter.requests[2]!
     expect(retryRequest.system).toBe(initialRequest.system)
     expect(nextStepRequest.system).toBe(initialRequest.system)
-    expect(retryRequest.messages.slice(0, -1)).toEqual(initialRequest.messages)
+    expect(retryRequest.messages.slice(0, -2)).toEqual(initialRequest.messages)
+    const failedAttempt = retryRequest.messages.at(-2)!
+    expect(failedAttempt).toMatchObject({
+      role: 'assistant',
+      content: [{ type: 'reasoning', text: `I should use the tool.\n${pseudoCall}` }],
+      source: { kind: 'model', provider: 'mock', model: 'mock' },
+    })
+    expect(failedAttempt.source.kind === 'model' && failedAttempt.source.replayState).toBeUndefined()
     const reminder = retryRequest.messages.at(-1)!
     expect(reminder.role).toBe('user')
     expect(reminder.source).toEqual({
@@ -275,19 +289,24 @@ describe('agent loop', () => {
       plugin: '@deepseek-ai/dsh-agent-loop/malformed-tool-call-retry',
     })
     const reminderText = reminder.content.find(block => block.type === 'text')?.text
-    expect(reminderText).toContain('structured tool-call channel')
+    expect(reminderText).toContain('No tool was executed for that attempt.')
+    expect(reminderText).toContain('native tool-call channel')
     expect(reminderText).toContain('If no tool is needed, continue reasoning or provide the final answer.')
     expect(nextStepRequest.messages.some(message => message.content.some(block =>
-      block.type === 'text' && block.text.includes('previous attempt ended with private reasoning')))).toBe(false)
+      block.type === 'text' && block.text.includes('No tool was executed for that attempt.')))).toBe(true)
     expect(recoveries).toBe(1)
     expect(executions).toBe(1)
+    expect(agent.session.events.filter(event => event.type === 'step/start').map(event => event.data.step))
+      .toEqual([1, 2, 3])
+    expect(agent.session.events.filter(event => event.type === 'assistant/message').map(event => event.data.step))
+      .toEqual([1, 2, 3])
     expect(agent.session.events.filter(event => event.type === 'tool/call')).toHaveLength(1)
     expect(agent.session.events.filter(event => event.type === 'assistant/message')
       .some(event => event.type === 'assistant/message'
         && event.data.message.content.some(block => block.type === 'reasoning' && block.text.includes(pseudoCall))))
-      .toBe(false)
+      .toBe(true)
     expect(agent.session.deriveMessages().some(message => message.content.some(block =>
-      block.type === 'text' && block.text.includes('previous attempt ended with private reasoning')))).toBe(false)
+      block.type === 'text' && block.text.includes('No tool was executed for that attempt.')))).toBe(true)
     expect(agent.session.events.findLast(event => event.type === 'turn/end')?.data.reason)
       .toEqual({ kind: 'completed' })
   })
@@ -313,12 +332,30 @@ describe('agent loop', () => {
     expect(adapter.requests).toHaveLength(2)
     expect(recoveries).toBe(1)
     expect(agent.session.events.some(event => event.type === 'tool/call')).toBe(false)
-    expect(agent.session.events.some(event => event.type === 'assistant/message')).toBe(false)
+    expect(agent.session.events.filter(event => event.type === 'assistant/message')).toHaveLength(2)
+    expect(agent.session.events.filter(event => event.type === 'assistant/message').map(event => event.data.step))
+      .toEqual([1, 2])
+    const [failedAttempt, reminder, repeatedAttempt] = agent.session.deriveMessages().slice(-3)
+    expect(failedAttempt).toMatchObject(
+      {
+        role: 'assistant',
+        content: [{ type: 'reasoning', text: pseudoCall }],
+      },
+    )
+    expect(reminder?.role).toBe('user')
+    expect(reminder?.content.find(block => block.type === 'text')?.text)
+      .toContain('No tool was executed for that attempt.')
+    expect(repeatedAttempt).toMatchObject(
+      {
+        role: 'assistant',
+        content: [{ type: 'reasoning', text: 'I will keep thinking about the task.' }],
+      },
+    )
     expect(agent.session.events.findLast(event => event.type === 'turn/end')?.data.reason).toEqual({
       kind: 'error',
       error: {
         code: 'MALFORMED_TOOL_CALL',
-        message: 'model "mock/mock" still returned only private reasoning after malformed tool-call recovery',
+        message: 'model "mock/mock" still returned only private reasoning after malformed tool-call recovery; no tool was executed',
       },
     })
   })
@@ -344,6 +381,12 @@ describe('agent loop', () => {
     expect(adapter.requests).toHaveLength(2)
     expect(recoveries).toBe(1)
     expect(agent.session.events.some(event => event.type === 'tool/call')).toBe(false)
+    expect(agent.session.events.filter(event => event.type === 'assistant/message').map(event => event.data.step))
+      .toEqual([1, 2])
+    expect(agent.session.deriveMessages().some(message => message.content.some(block =>
+      block.type === 'reasoning' && block.text === pseudoCall))).toBe(true)
+    expect(agent.session.deriveMessages().some(message => message.content.some(block =>
+      block.type === 'text' && block.text.includes('No tool was executed for that attempt.')))).toBe(true)
     expect(agent.session.deriveMessages().at(-1)?.content).toEqual([
       { type: 'reasoning', text: 'No tool is needed after reconsidering the task.' },
       { type: 'text', text: 'Here is the final answer.' },
diff --git a/packages/llm/llm-pi-ai/tests/adapter.spec.ts b/packages/llm/llm-pi-ai/tests/adapter.spec.ts
index 8f322abbe4..97b7a1a5e8 100644
--- a/packages/llm/llm-pi-ai/tests/adapter.spec.ts
+++ b/packages/llm/llm-pi-ai/tests/adapter.spec.ts
@@ -9,7 +9,7 @@ import type {
   SaveImageAttachment,
   StoredImageAttachment,
 } from '@deepseek-ai/dsh-attachment'
-import LlmRuntime, { createUserMessage, CONTEXT_WINDOW_EXCEEDED_CODE, LlmError, ReasoningEffortId, userAgent } from '@deepseek-ai/dsh-llm'
+import LlmRuntime, { createAssistantMessage, createUserMessage, CONTEXT_WINDOW_EXCEEDED_CODE, LlmError, ReasoningEffortId, userAgent } from '@deepseek-ai/dsh-llm'
 import * as LlmPiAi from '@deepseek-ai/dsh-llm-pi-ai'
 import { PiAiAdapter } from '@deepseek-ai/dsh-llm-pi-ai'
 import { MAX_TIMER_DELAY_MS } from '@deepseek-ai/dsh-timeout'
@@ -101,6 +101,38 @@ describe('PiAiAdapter provider routing', () => {
     expect(second.requests).toHaveLength(0)
   })
 
+  it('replays provider-neutral reasoning-only assistant history before a user-role reminder', async () => {
+    const server = await mockServer([{ events: textEvents }])
+    const ctx = await harness(server.url)
+    await assemble(ctx, {
+      model: 'deepseek-v4-flash',
+      messages: [
+        createUserMessage({
+          content: [{ type: 'text', text: 'use the tool' }],
+          source: { kind: 'plugin', plugin: 'test' },
+        }),
+        createAssistantMessage({
+          content: [{ type: 'reasoning', text: '<tool_call>invalid</tool_call>' }],
+          // No replayState is deliberate: malformed attempts retain their
+          // durable reasoning without asking pi-ai to trust native replay data.
+          source: { provider: 'deepseek', model: 'deepseek-v4-flash' },
+        }),
+        createUserMessage({
+          content: [{ type: 'text', text: '<system-reminder>No tool was executed.</system-reminder>' }],
+          source: { kind: 'plugin', plugin: 'test-retry' },
+        }),
+      ],
+    })
+
+    expect(server.requests[0]).toMatchObject({
+      messages: [
+        { role: 'user', content: 'use the tool' },
+        { role: 'assistant', content: '<tool_call>invalid</tool_call>' },
+        { role: 'user', content: '<system-reminder>No tool was executed.</system-reminder>' },
+      ],
+    })
+  })
+
   it('merges profile headers with Harness attribution winning', async () => {
     const server = await mockServer([{ events: textEvents }])
     const ctx = await harness(server.url, {
diff --git a/packages/llm/llm-retry/README.i18n.yaml b/packages/llm/llm-retry/README.i18n.yaml
index 42f2e7477d..226e9c2a9c 100644
--- a/packages/llm/llm-retry/README.i18n.yaml
+++ b/packages/llm/llm-retry/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/llm/llm-retry/README.md
-README.md: fb48f12fb9448f28f64f4715ccc4b90b96a522a6
-README.zh.md: b210f946b0c870d15baa0e25edce6fda84e5bd0a
+README.md: 8c4469c30a9ae4fbe334c303cecb57cce753b6cd
+README.zh.md: 0c7658f3f0aeabbab294d83660f9c209228a8512
diff --git a/packages/llm/llm-retry/README.md b/packages/llm/llm-retry/README.md
index fb48f12fb9..8c4469c30a 100644
--- a/packages/llm/llm-retry/README.md
+++ b/packages/llm/llm-retry/README.md
@@ -4,7 +4,7 @@ English | [中文](README.zh.md)
 
 Function plugin that applies exact-provider retry policy through the agent loop's closed-step `agent/request-error` waterfall. It does not wrap `ctx.llm.stream()`: every adapter call remains one provider attempt, and every retry opens a fresh numbered turn.
 
-Each provider adapter owns an optional nested `retryPolicy`, captured when its route registers on `ctx.llm` and carried with each call that reaches that registration's final adapter boundary. An in-flight failure retains that serving policy if the route is later disposed or replaced; a failure before any final adapter is selected has no provider policy and delegates. Omission uses normal mode: five retries for `EMPTY_RESPONSE`, `MALFORMED_TOOL_CALL`, `RATE_LIMIT`, `SERVER`, `TIMEOUT`, and `TRANSPORT`, with bounded exponential backoff from 500 ms to 10 seconds and 10 percent jitter. `EMPTY_RESPONSE` is the adapters' classification of a degenerate provider completion that produced no durable content, so repeating it is safe. The agent loop offers `MALFORMED_TOOL_CALL` only for the first reasoning-markup defect in a step, adds a transient corrective request-tail message while keeping the system prompt and durable history prefix unchanged, and fails a repeated reasoning-only result without offering it again. A normal policy can change its finite budget, eligible codes, and backoff. Always mode asks downstream recovery first, then retries every model-request failure without an attempt limit; success, cancellation, or plugin disposal stops it after active delegated recovery reaches quiescence.
+Each provider adapter owns an optional nested `retryPolicy`, captured when its route registers on `ctx.llm` and carried with each call that reaches that registration's final adapter boundary. An in-flight failure retains that serving policy if the route is later disposed or replaced; a failure before any final adapter is selected has no provider policy and delegates. Omission uses normal mode: five retries for `EMPTY_RESPONSE`, `MALFORMED_TOOL_CALL`, `RATE_LIMIT`, `SERVER`, `TIMEOUT`, and `TRANSPORT`, with bounded exponential backoff from 500 ms to 10 seconds and 10 percent jitter. `EMPTY_RESPONSE` is the adapters' classification of a degenerate provider completion that produced no durable content, so repeating it is safe. The agent loop offers `MALFORMED_TOOL_CALL` only for the initial reasoning-markup defect, retains that invalid assistant attempt, and durably follows it with one user-role corrective reminder after recovery is accepted. The corrective request runs in a new step, preserving the failed Think in the transcript. The original request remains an exact prefix; a repeated reasoning-only result is retained and then fails without another offer. A normal policy can change its finite budget, eligible codes, and backoff. Always mode asks downstream recovery first, then retries every model-request failure without an attempt limit; success, cancellation, or plugin disposal stops it after active delegated recovery reaches quiescence.
 
 Both modes use bounded exponential backoff with symmetric jitter. A valid `providerRetryAfterMs` at or below `maxDelayMs` replaces local backoff without jitter. An over-cap provider delay makes normal mode delegate, while always mode uses its configured local backoff so it cannot terminate on that instruction.
 
diff --git a/packages/llm/llm-retry/README.zh.md b/packages/llm/llm-retry/README.zh.md
index b210f946b0..0c7658f3f0 100644
--- a/packages/llm/llm-retry/README.zh.md
+++ b/packages/llm/llm-retry/README.zh.md
@@ -4,7 +4,7 @@
 
 一个函数插件，通过 agent loop（智能体循环）在已关闭步骤上触发的 `agent/request-error` waterfall（瀑布式事件）应用确切提供方重试策略。它不包装 `ctx.llm.stream()`：每次适配器调用仍是一次提供方尝试，每次重试都会开启新的编号轮次。
 
-每个提供方适配器都拥有可选的嵌套 `retryPolicy`；路由在 `ctx.llm` 上注册时会捕获该策略，任何到达该注册最终适配器边界的调用都会携带它。如果之后释放或替换路由，进行中的失败仍会保留当时为其提供服务的策略；在选中任何最终适配器前发生的失败没有提供方策略，会继续委托。省略策略时使用 normal mode：为 `EMPTY_RESPONSE`、`MALFORMED_TOOL_CALL`、`RATE_LIMIT`、`SERVER`、`TIMEOUT` 和 `TRANSPORT` 重试五次，并采用从 500 ms 到 10 秒的有界指数退避与 10% jitter。`EMPTY_RESPONSE` 是适配器对未产生任何持久内容的退化提供方完成所作的分类，因此可安全重复。agent loop 只会在一个步骤首次出现 reasoning 工具标记缺陷时提供 `MALFORMED_TOOL_CALL`，为该次重试临时追加纠错请求尾消息，同时保持 system prompt 与持久历史前缀不变；若再次只返回 reasoning，则不会再次进入恢复。normal 策略可以更改其有限预算、符合条件的 code 和退避配置。always mode 会先请求下游恢复，再无次数上限地重试每个模型请求失败；成功、取消或插件 dispose（资源释放）会在活跃的委托恢复完全停稳后终止它。
+每个提供方适配器都拥有可选的嵌套 `retryPolicy`；路由在 `ctx.llm` 上注册时会捕获该策略，任何到达该注册最终适配器边界的调用都会携带它。如果之后释放或替换路由，进行中的失败仍会保留当时为其提供服务的策略；在选中任何最终适配器前发生的失败没有提供方策略，会继续委托。省略策略时使用 normal mode：为 `EMPTY_RESPONSE`、`MALFORMED_TOOL_CALL`、`RATE_LIMIT`、`SERVER`、`TIMEOUT` 和 `TRANSPORT` 重试五次，并采用从 500 ms 到 10 秒的有界指数退避与 10% jitter。`EMPTY_RESPONSE` 是适配器对未产生任何持久内容的退化提供方完成所作的分类，因此可安全重复。agent loop 只会为最初一次 reasoning 工具标记缺陷提供 `MALFORMED_TOOL_CALL`，保留该次无效 assistant 尝试，并在恢复被接受后持久紧跟一条 user-role 纠错 reminder。纠错请求在新步骤中运行，因此失败 Think 会保留在 transcript 中。原请求保持为精确前缀；若再次只返回 reasoning，该结果会被保留，随后失败且不再提供恢复机会。normal 策略可以更改其有限预算、符合条件的 code 和退避配置。always mode 会先请求下游恢复，再无次数上限地重试每个模型请求失败；成功、取消或插件 dispose（资源释放）会在活跃的委托恢复完全停稳后终止它。
 
 两种 mode 都使用带对称 jitter 的有界指数退避。有效 `providerRetryAfterMs` 不超过 `maxDelayMs` 时会替换本地退避，并且不加 jitter。超出上限的提供方延迟会使 normal mode 继续委托；always mode 则改用已配置的本地退避，避免该指令终止重试。
 
diff --git a/packages/llm/llm-retry/tests/retry.spec.ts b/packages/llm/llm-retry/tests/retry.spec.ts
index 6077d9f529..e8ba5c55ea 100644
--- a/packages/llm/llm-retry/tests/retry.spec.ts
+++ b/packages/llm/llm-retry/tests/retry.spec.ts
@@ -269,7 +269,7 @@ describe('provider-routed retry policy', () => {
     })
   })
 
-  it('retries malformed reasoning tool markup with a transient request-tail reminder', async () => {
+  it('retries malformed reasoning tool markup with durable failed-attempt context', async () => {
     vi.useFakeTimers()
     const adapter = new ScriptedAdapter([
       reasoningToolMarkupResponse(),
@@ -287,7 +287,7 @@ describe('provider-routed retry policy', () => {
 
     expect(event.data.failure).toEqual({
       code: 'MALFORMED_TOOL_CALL',
-      message: 'model "mock/mock" wrote tool-call markup in reasoning instead of emitting a structured tool call',
+      message: 'model "mock/mock" wrote tool-call markup in reasoning; no tool was executed',
     })
     expect(adapter.requests).toHaveLength(1)
 
@@ -299,7 +299,11 @@ describe('provider-routed retry policy', () => {
     const initialRequest = adapter.requests[0]!
     const retryRequest = adapter.requests[1]!
     expect(retryRequest.system).toBe(initialRequest.system)
-    expect(retryRequest.messages.slice(0, -1)).toEqual(initialRequest.messages)
+    expect(retryRequest.messages.slice(0, -2)).toEqual(initialRequest.messages)
+    expect(retryRequest.messages.at(-2)).toMatchObject({
+      role: 'assistant',
+      content: [expect.objectContaining({ type: 'reasoning' })],
+    })
     const reminder = retryRequest.messages.at(-1)!
     expect(reminder.role).toBe('user')
     expect(reminder.source).toEqual({
@@ -308,12 +312,14 @@ describe('provider-routed retry policy', () => {
     })
     const reminderText = reminder.content.find(block => block.type === 'text')?.text
     expect(reminderText).toContain('<system-reminder>')
+    expect(reminderText).toContain('No tool was executed for that attempt.')
     expect(reminderText).toContain('Do not write tool-call XML, JSON, tags, or arguments')
     expect(reminderText).toContain('If no tool is needed, continue reasoning or provide the final answer.')
     expect(agent.session.events.filter(item => item.type === 'llm/retry')).toHaveLength(1)
+    expect(agent.session.events.filter(item => item.type === 'assistant/message')).toHaveLength(2)
     expect(agent.session.deriveMessages().at(-1)?.content).toEqual([{ type: 'text', text: 'recovered' }])
     expect(agent.session.deriveMessages().some(message => message.content.some(block =>
-      block.type === 'text' && block.text.includes('<system-reminder>')))).toBe(false)
+      block.type === 'text' && block.text.includes('<system-reminder>')))).toBe(true)
   })
 
   it('leaves partial failed chunks on their step without committing a message or tool side effect', async () => {
diff --git a/packages/llm/llm/README.i18n.yaml b/packages/llm/llm/README.i18n.yaml
index 3b7978d8a9..1f88709def 100644
--- a/packages/llm/llm/README.i18n.yaml
+++ b/packages/llm/llm/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/llm/llm/README.md
-README.md: ed151edfcf6cc3cdde1f0873f57445d17b6070ff
-README.zh.md: bdec60a86f12df34184e299b7e128c91002795d2
+README.md: 599aea65c215644d11c8c8646f9cbad4230ddacc
+README.zh.md: f80e88d48496b3f22856824bd1eae4a221756b0f
diff --git a/packages/llm/llm/README.md b/packages/llm/llm/README.md
index ed151edfcf..599aea65c2 100644
--- a/packages/llm/llm/README.md
+++ b/packages/llm/llm/README.md
@@ -83,7 +83,7 @@ Every adapter that puts a credential in an HTTP header judges it the same way be
 - `CONTEXT_WINDOW_EXCEEDED_CODE` — the provider-neutral code both DeepSeek adapters use when a request exceeds the model context window, regardless of thrown-HTTP versus in-band finish delivery. `isContextWindowExceededError(detail)` is their shared conservative classifier for OpenAI-compatible provider detail.
 - `QUOTA_EXCEEDED_CODE` — the non-transient provider-neutral code for exhausted account quota, balance, credits, budget, or usage limits. `isQuotaExceededError(detail)` keeps those failures distinct from request-rate limits.
 - `EMPTY_RESPONSE_CODE` — the provider-neutral code both adapters use for a degenerate provider completion: a terminal `stop` that carried no content blocks at all. Classified as an error finish (not a successful empty message) because the attempt produced nothing durable; `dsh-llm-retry` retries it by default.
-- `MALFORMED_TOOL_CALL_CODE` — the provider-neutral code the agent loop uses when a normal `stop` contains Qwen-style tool-call markup only in private reasoning, with no visible answer or native structured call. The markup is never parsed or executed. The default retry path permits one corrective retry; a repeated reasoning-only result ends explicitly with this code.
+- `MALFORMED_TOOL_CALL_CODE` — the provider-neutral code the agent loop uses when a normal `stop` contains Qwen-style tool-call markup only in private reasoning and no tool was executed. The markup is never parsed or executed. The invalid assistant attempt and its user-role corrective reminder remain in durable history; the default path permits one retry, and a repeated reasoning-only result is retained before ending explicitly with this code.
 - `INVALID_CREDENTIAL_CODE` — the provider-neutral code for a credential that was supplied but cannot be used: malformed rather than absent, so the fix is to correct the stored value rather than supply one — the distinction from `MISSING_CREDENTIAL`. Deliberately excluded from the default retryable set, since a malformed credential fails identically on every attempt. `assertUsableApiKey(raw, pkg, ref)` throws `LlmError` with this code, the one shared diagnosis every adapter uses for an unusable stored credential.
 
 ### Real adapters
diff --git a/packages/llm/llm/README.zh.md b/packages/llm/llm/README.zh.md
index bdec60a86f..f80e88d484 100644
--- a/packages/llm/llm/README.zh.md
+++ b/packages/llm/llm/README.zh.md
@@ -85,7 +85,7 @@
 - `CONTEXT_WINDOW_EXCEEDED_CODE`：当请求超过模型上下文窗口时，无论通过 HTTP 异常抛出还是带内 finish 交付，两个 DeepSeek 适配器都使用的提供方无关 code。`isContextWindowExceededError(detail)` 是它们针对 OpenAI 兼容提供方详细信息的共享保守分类器。
 - `QUOTA_EXCEEDED_CODE`：帐户配额、余额、点数、预算或用量限制耗尽时使用的非暂时性提供方无关 code。`isQuotaExceededError(detail)` 使这些失败与请求速率限制保持区分。
 - `EMPTY_RESPONSE_CODE`：两个适配器都使用的提供方无关 code，用于表示退化的提供方生成结果：一个未携带任何内容块的终止 `stop`。它会被分类为错误 finish（而非成功空消息），因为尝试未产生持久内容；`dsh-llm-retry` 默认重试它。
-- `MALFORMED_TOOL_CALL_CODE`：agent loop 在普通 `stop` 只把 Qwen 风格工具调用标记写进私有 reasoning、既无可见回答也无原生结构化调用时使用的提供方无关 code。该标记绝不会被解析或执行。默认恢复路径允许一次纠错重试；若再次只返回 reasoning，则以此 code 明确结束。
+- `MALFORMED_TOOL_CALL_CODE`：agent loop 在普通 `stop` 只把 Qwen 风格工具调用标记写进私有 reasoning、且没有工具被执行时使用的提供方无关 code。该标记绝不会被解析或执行。无效 assistant 尝试及其 user-role 纠错 reminder 会保留在持久历史中；默认恢复路径允许一次重试，若再次只返回 reasoning，该结果会先被保留，再以此 code 明确结束。
 - `INVALID_CREDENTIAL_CODE`：已提供但无法使用的凭据所用的提供方无关 code——格式错误而非缺失，修复方式是改正已存储的值，而不是补充一个凭据，这正是它与 `MISSING_CREDENTIAL` 的区别。它被刻意排除在默认可重试集合之外：格式错误的凭据每次尝试都会以同样方式失败。`assertUsableApiKey(raw, pkg, ref)` 会以该 code 抛出 `LlmError`，是每个适配器判定已存储凭据不可用时共用的诊断。
 
 ### 真实适配器
diff --git a/packages/llm/llm/src/error.ts b/packages/llm/llm/src/error.ts
index f76ae3715c..e61898e162 100644
--- a/packages/llm/llm/src/error.ts
+++ b/packages/llm/llm/src/error.ts
@@ -40,10 +40,10 @@ export const EMPTY_RESPONSE_CODE = 'EMPTY_RESPONSE'
 
 /**
  * Canonical provider-neutral code for a completed model response that put a
- * prospective tool invocation in private reasoning instead of emitting a
- * structured tool call. The agent loop may retry this semantic format failure
- * once with corrective guidance, but must never parse or execute the reasoning
- * text itself.
+ * prospective tool invocation in private reasoning. No tool was executed for
+ * that attempt. The agent loop may retain the invalid assistant message and
+ * retry this semantic format failure once with corrective guidance, but must
+ * never parse or execute the reasoning text itself.
  */
 export const MALFORMED_TOOL_CALL_CODE = 'MALFORMED_TOOL_CALL'
 
-- 
2.51.2.windows.1

