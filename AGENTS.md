你正在参与 Deepseek Harness 项目的研发。请严格遵守以下项目协作规则，并将其作为本次对话中所有开发工作的默认执行方式。

一、系统分工

1. DP 平台是项目研发管理的唯一事实来源：
   - 当前项目固定为：Deepseek Harness。
   - 需求、研发任务、负责人、Bug、测试用例、测试计划、版本发布、研发进度和文档关联，都必须记录在 DP 平台。
   - 使用已安装的 developer-platform-cli Skill 和 dp.exe 操作 DP。
   - 只要 dp.exe 已支持，就不能绕过 CLI 直接调用 API、操作数据库或复用浏览器 Cookie。

2. 飞书是项目文档内容的主要载体：
   - 产品方案、技术设计、调研报告、测试报告、发布说明、复盘等正式文档，优先存放在飞书。
   - 使用相应的 lark-* Skill 和 lark-cli 读取、创建或更新飞书文档。
   - DP 项目文档库负责保存文档索引和关联关系，不重复复制飞书正文。
   - Git 仓库中的 docs 用于保存代码紧密相关、需要版本控制的文档，以及必要的研发和部署记录。

3. Git 仓库是代码和配置的唯一事实来源：
   - 不覆盖用户已有的未提交修改。
   - 开发前检查当前分支、工作区状态和相关项目文档。
   - 所有提交必须能追溯到对应的 DP 需求或 Bug 编号。

二、每次开始开发前

1. 阅读项目中的 AGENTS.md、README、相关技术文档和部署说明。
2. 检查 Git 当前分支、未提交修改和远端同步状态。
3. 按 developer-platform-cli Skill 的要求检查：
   - dp --version
   - dp doctor --json
   - dp auth status --json
4. 确认当前 DP 项目是 DSH（Deepseek Harness）；如果不是，先通过 dp project use 切换。
5. 如果用户给出了需求或 Bug 编号，先查询并理解它的：
   - 当前状态
   - 验收标准
   - 负责人和参与人
   - 研发任务
   - 关联文档
   - 测试计划
   - 发布版本
   - allowed_transitions
6. 如果用户明确要求开发一个新功能但没有 DP 需求：
   - 先在 DP 中搜索是否已有重复需求。
   - 没有重复项时，整理标题、背景、目标、范围、验收标准和优先级，再创建需求。
   - 只有确实会影响实现方向的关键信息不明确时，才向用户提出一个简洁问题；其他信息应根据上下文合理补全并说明假设。
7. 如果是缺陷修复，则优先关联或创建对应 Bug，并记录复现方式、预期结果和实际结果。

三、构建、启动与源码一致性（强制）

1. 运行时只能从同一份锁定源码构建和启动，不能让构建目录、启动脚本和 profile 各自指向不同版本：
   - `upstream.lock.json` 是官方源码基线、补丁顺序、Node/pnpm 版本和最终 Git tree 的唯一锁定来源。
   - `D:\Pythonproject\deepseek-harness` 是源码 checkout；其同级的
     `deepseek-harness-runtime` 是干净构建 worktree，只用于安装依赖、完整构建和启动。
   - `restart-dsh-web.ps1` 是 Windows 本地启动的唯一标准入口；`restart-dsh-web.bat` 只负责调用它，
     不得在 bat 文件中另维护一套启动参数或源码路径。
   - `~/.dsh/profiles/web` 是运行配置，`~/.dsh/logs` 中的 build/PID 元数据和启动日志是运行态证据。

2. 小改动不豁免一致性检查。只要改动会进入运行时，包括一个插件的 TypeScript/CSS、生成的 `lib`、
   `package.json`、Cordis patch、profile、上游补丁、启动脚本或构建脚本，都必须重新构建对应产物，
   并在交付前至少执行一次完整的本地重启验证。纯文档或不影响运行时的记录修改可以跳过构建，但
   不得以此为理由跳过代码变更的构建检查。

3. 每次启动前必须执行以下门禁，任何一项失败都停止启动，不得复用旧 `lib` 或旧 release 目录：
   - 校验源码 checkout 的 `HEAD^{tree}` 等于 `upstream.lock.json` 的 `resultTree`；已跟踪文件有修改时
     直接停止。未跟踪的临时检查文件可以保留，但构建必须在干净 runtime worktree 中进行。
   - 校验 Node 和 pnpm 版本与锁文件一致，执行 `pnpm install --frozen-lockfile` 和完整的
     `pnpm run build`。
   - 校验 CLI、Host apiproxy、vision-bridge 的构建产物存在，并确认 Host/ACP 的
     `imageInputBridge` 准入链路仍在构建结果中。
   - 执行 `dsh --profile web --dump-config`，确认 vision-bridge 的 `disabled: false`、DP Gateway
     配置和默认插件行存在，确认已退役的 `image-context-guard` 不存在。
   - 从同一个 runtime worktree 启动 CLI，校验 HTTP 200、监听 PID、实际命令行、skill-manager/plugin-manager
     API 版本和构建元数据。只看到构建成功、没有看到实际启动成功，不能声明本地服务可用。

4. 不同改动的最小执行范围如下：
   - 插件 Host、插件 Client、插件依赖或插件 profile：先在 `dsharness` 执行
     `pnpm install --frozen-lockfile`、`pnpm run verify`，再运行 `.\restart-dsh-web.ps1`。
     插件 `src` 是源码真相，`lib` 是生成产物，禁止手工编辑 `lib` 代替构建。
   - 上游源码、上游补丁、锁文件或 DSH 核心构建配置：执行
     `.\dev\install-dsh-source.ps1` 或完整的 `.\dev\verify-dsh-source.ps1`，命中锁定 tree 后再运行
     `.\restart-dsh-web.ps1`。
   - profile、插件 link/junction、构建脚本或启动脚本：除脚本解析测试外，必须完成一次真实启动，
     并核对 `~/.dsh/logs/dsh-web-<port>.build.json` 与 PID 元数据中的源码 HEAD、tree、构建目录和 CLI 路径。

5. 发生构建与启动不一致时，按以下顺序处理：
   - 先查看 `git status`、`upstream.lock.json`、build/PID 元数据以及本次启动 stderr，确认实际使用的
     源码、构建目录和 CLI 路径；不要凭文件时间或目录名称判断版本。
   - 重新运行锁定源码校验、frozen install、完整构建和启动验证。若是插件加载失败，修复插件源码并重新
     生成 `lib`，不得为了让服务启动而静默禁用插件或绕过 profile 门禁；临时禁用必须有明确授权和记录。
   - 如果源码目录存在用户未提交修改，不得 reset、clean、覆盖或删除；使用新的干净 runtime worktree，
     并在交付中说明这些未跟踪内容没有参与构建。

6. 构建、启动脚本必须与源码和 profile 同步维护。修改其中任一项后，应同时检查另外两项的路径、参数、
   版本校验、产物校验和错误处理，并补充相应的自动化测试；不得只修启动脚本而继续引用旧构建产物，
   也不得只更新源码而保留旧的安装/启动命令。

四、开发执行规则

1. 开始实现时，在 DP 中建立或更新研发任务，设置总负责人和各阶段负责人，并同步真实研发状态。
2. 分支名称应包含 DP 编号，例如：
   - feat/CAE-010-multimodal-research
   - fix/BUG-001-page-refresh
3. 默认流程是：
   - 功能分支开发
   - 本地验证
   - 合并到 sit
   - SIT 环境测试
   - 确认通过后合并 main
   - 部署生产
   - 生产冒烟验证
4. 未经用户明确授权，不得直接合并 main 或部署生产。
5. 如果用户已经明确说“完成后上线”“测试通过后直接部署”等，则视为已授权完整发布流程；测试通过后可以继续执行，不必重复询问。
6. 不得绕过 DP 中的权限、版本冲突、状态流转和发布条件：
   - 状态变化前先读取 allowed_transitions。
   - 遇到 409 冲突时重新读取对象，确认用户意图仍然成立后再重试。
   - 401 表示需要重新登录。
   - 403 表示权限不足，不能当作登录失败处理。
7. 每个写操作都遵循：
   - 操作前查询
   - 执行修改
   - 操作后重新查询验证
8. 调用 dp.exe 时，供程序读取的输出统一使用 --json。参数不确定时先查看命令帮助，不猜测参数或状态值。

五、测试与质量要求

1. 功能需求至少覆盖：
   - 正常流程
   - 必填及边界条件
   - 权限或状态限制
   - 失败处理
   - 关键回归场景
2. 测试用例和测试计划应登记在 DP，并关联到对应需求。
3. 测试发现的问题应创建或关联 DP Bug，修复后补充回归结果。
4. 前端功能需要进行浏览器实际操作验证；浏览器主要用于 UI 和交互验收，不代替 dp.exe 进行 DP 业务数据操作。
5. 不能执行某项测试时，必须说明：
   - 未执行的测试
   - 具体阻塞原因
   - 已完成的替代验证
   - 剩余风险
6. 不得在没有证据的情况下声称“测试通过”或“已经上线”。

六、飞书文档与 DP 联动

1. 为某个需求创建、查找或更新飞书文档时，必须取得：
   - 飞书文档标题
   - 规范化后的文档 URL
   - 明确的 DP 需求编号
2. Wiki 链接需要先通过 lark-cli 检查，识别底层真实文档。
3. 完成飞书操作后，执行类似下面的关联操作：

   dp document ensure --url <飞书文档URL> --title <文档标题> --requirement <需求编号> --json

4. 随后查询 DP 需求，确认文档已经关联。
5. 不得仅根据标题或主题相似度猜测需求关系。
6. 如果飞书文档创建成功但 DP 关联失败：
   - 保留已创建的飞书文档。
   - 只重试 DP document ensure。
   - 不得重复创建飞书文档。
7. 更新已有文档关联时，应保留原有人工填写的标题、摘要、分类、标签和其他关联。
8. 飞书访问令牌、DP PAT、密码等敏感信息不得写入命令参数、代码、日志、DP 文档或飞书文档。

七、研发记录与交付

1. 每次实现、排查或重要调整后，同步更新：
   D:\Obsidian\gysnote\项目训练\DSH
2. 根据本次工作更新必要的：
   - 需求或 Bug
   - 研发任务状态
   - 测试用例和测试计划
   - 飞书技术文档
   - DP 文档关联
   - 发布版本
   - Git 提交
   - 部署及回滚记录
3. 不要为了形式创建大量空文档；只维护对后续研发、测试、发布或交接确实有价值的内容。

八、完成标准

只有同时满足以下条件，才能声明本次研发完成：

1. 代码实现符合需求和验收标准。
2. 本地自动化测试、必要的浏览器测试及 SIT 验证均已通过。
3. DP 中的需求、任务、测试、Bug、文档和发布状态与真实情况一致。
4. 相关飞书文档已经创建或更新，并正确关联到 DP 需求。
5. Git 提交清晰且包含对应 DP 编号。
6. 如果获得生产发布授权：
   - 已合并 main
   - 已部署生产
   - 已完成生产健康检查和核心功能冒烟测试
   - 已记录部署版本、提交号、备份及回滚信息
7. 没有被隐瞒的失败步骤或未说明风险。

九、最终汇报格式

任务结束时，用简洁方式报告：

- DP 需求或 Bug 编号及最终状态
- 完成的主要改动
- Git 分支和提交号
- 执行的测试及结果
- 飞书文档及 DP 关联情况
- SIT/生产部署结果
- 剩余风险或后续事项

除非遇到会实质改变需求、造成数据破坏、涉及权限边界或无法安全判断的问题，否则请主动推进完整流程，不要反复向用户确认可以通过现有信息确定的细节。

Appendix:
一些 Deepseek Harness 的写入工具(write,edit)使用建议
   - 当要生成一个很大的文件（不限文档、代码）时，建议先用 write 生成大致框架，再使用 edit 进行局部的完善
   - 当要修改一个很大的文件（不限文档、代码）时，使用 edit 进行多次的局部修改
